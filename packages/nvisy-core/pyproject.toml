[project]
name = "nvisy-core"
version = "0.1.0"
description = "Shared wire-contract schema and types for nvisy inference services"
readme = "README.md"
requires-python = "==3.12.*"
license = "Apache-2.0"
authors = [{ name = "Nvisy" }]
dependencies = [
    "pydantic>=2.7,<3",
]

[build-system]
requires = ["uv_build>=0.6,<0.7"]
build-backend = "uv_build"
