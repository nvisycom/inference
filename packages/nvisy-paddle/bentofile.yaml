# Build from the repo root:  bentoml build -f packages/nvisy-paddle/bentofile.yaml .
# Image config (Python version, system packages, requirements) lives in
# service.py; this file selects the service and scopes the bundled source.
service: "nvisy_paddle.service:OcrVlService"
include:
  - "packages/nvisy-paddle/**"
  - "packages/nvisy-core/**"
  - "pyproject.toml"
  - "uv.lock"
