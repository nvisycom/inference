# Build from the repo root:  bentoml build -f packages/nvisy-doctr/bentofile.yaml .
# Image config (Python version, system packages, requirements) lives in
# service.py; this file selects the service and scopes the bundled source.
service: "nvisy_doctr.service:OcrService"
include:
  - "packages/nvisy-doctr/**"
  - "packages/nvisy-core/**"
  - "pyproject.toml"
  - "uv.lock"
