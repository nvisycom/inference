# proto

Versioned JSON schemas for the nvisy inference wire contract — the **product
spec** both the Python services and the Rust runtime client mirror.

> **Status:** placeholder. Schemas (`ocr/v1.json`, `ner/v1.json`) are authored
> in the contract-design follow-up. Per the ADR, the contract is *our* shape,
> not BentoML's, and the runtime (`nvisy-inference-client`) is the source of
> truth that these schemas are kept in sync with.
